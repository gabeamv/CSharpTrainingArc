<UserControl x:Class="SecureNotes.Views.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SecureNotes.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
            <Button Content="Go Home" Width="75" Height="20" Command="{Binding NavigateHome}" Margin="0, 0, 10, 0"/>
            <Button Content="Refresh Users" Width="80" Height="20" Command="{Binding LoadUsers}" Margin="10, 0, 0, 0"/>
            <TextBox Text="{Binding FeedbackMessage}" Height="20" Width="200" Margin="20, 0 ,0, 0"/>
        </StackPanel>

        <ComboBox Grid.Row="1" ItemsSource="{Binding Users}" Height="25" VerticalAlignment="Top" SelectedItem="{Binding Recipient}" Margin="10, 0, 10, 0">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Username}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
