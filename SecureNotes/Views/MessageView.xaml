<UserControl x:Class="SecureNotes.Views.MessageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SecureNotes.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
            <Button Content="Go Home" Width="75" Height="20" Command="{Binding NavigateHome}" Margin="0, 0, 10, 0"/>
            <Button Content="Refresh Users" Width="80" Height="20" Command="{Binding LoadUsers}" Margin="10, 0, 0, 0"/>
            <Button Content="Load Messages" Width="100" Height="20" Command="{Binding LoadMessages}" Margin="10, 0, 0, 0"/>
            <TextBox Text="{Binding FeedbackMessage}" Height="20" Width="200" Margin="20, 0 ,0, 0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Width="500">
            <ComboBox ItemsSource="{Binding Users}" Height="25" VerticalAlignment="Top" SelectedItem="{Binding Recipient}" Margin="10, 0, 10, 0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Username}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Send Files" Name="Button_SendPayload" Width="100" Height="20" Margin="0, 20, 0, 0" Command="{Binding SendFiles}"/>
            <ComboBox ItemsSource="{Binding UserMessages}" Height="25" VerticalAlignment="Top" SelectedItem="{Binding SelectedMessage}" Margin="10, 20, 10, 0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="From "/>
                            <TextBlock Text="{Binding Sender}"/>
                            <TextBlock Text=" @"/>
                            <TextBlock Text="{Binding Timestamp}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        
    </Grid>
</UserControl>
