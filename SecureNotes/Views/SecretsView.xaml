<UserControl x:Class="SecureNotes.Views.SecretsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SecureNotes.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Orientation="Vertical">
            <Button Content="Home" Height="20" Width="50" Margin="0, 10, 0, 0" Command="{Binding NavigateHome}"/>
            <TextBox Width="400" Height="250" Margin="0, 10, 0 , 0" Text="{Binding SecretMessage}" TextWrapping="Wrap"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="Save" Height="20" Width="50" Margin="0, 30, 15, 0" Command="{Binding SaveCommand}"/>
                <Button Content="Read" Height="20" Width="50" Margin="15, 30, 0, 0" Command="{Binding ReadCommand}"/>
            </StackPanel>
            
        </StackPanel>
    </Grid>
</UserControl>
